options gen2

// Mouse Rotate - World Component (daslang)
// First-person mouse rotation for an entity.

struct MouseRotate {
    sensitivity : float = 0.1
    yaw : float = 0.0
    pitch : float = 0.0
    pitch_min : float = -89.0
    pitch_max : float = 89.0
}
