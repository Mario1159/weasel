#include <wsl/ai/a2a/agent_card.hpp>

// agent_card types are plain data structures with no behavior.
// This translation unit exists to ensure the header compiles and to
// provide a place for future non-trivial member functions if needed.

namespace wsl::ai::a2a
{

} // namespace wsl::ai::a2a
